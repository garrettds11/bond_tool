import { defineConfig } from 'vite';

// Basic config; Vite's built-in React transform via esbuild is enough for simple cases,
// but for full React Fast Refresh features you can install @vitejs/plugin-react.
export default defineConfig({});
